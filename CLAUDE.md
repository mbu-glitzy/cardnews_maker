# CLAUDE.md — 개발 프로젝트 마스터 파일

> 모든 개발 세션 시작 시 이 파일을 먼저 읽습니다.
> 프로젝트별 도메인/산업 컨텍스트는 세션 시작 시 별도로 전달됩니다.

---

## 1. 기술 스택 기본값

### 고정 스택 (모든 프로젝트 공통)
- **Backend/DB**: Supabase (PostgreSQL + Auth + Storage + Edge Functions)
- **Hosting**: Vercel (자동 배포, Preview URL)
- **Version Control**: Git → GitHub → Vercel 자동 배포

### 프론트엔드 기본값
- **Framework**: Next.js (App Router 기준)
- **Styling**: Tailwind CSS
- **Language**: TypeScript

> 위와 다른 스택을 써야 할 경우, 세션 시작 시 명시적으로 알려줍니다.
> 명시가 없으면 위 스택을 기본으로 가정하고 작업합니다.

### Supabase 작업 원칙
- RLS(Row Level Security) 항상 활성화 — 예외 없음
- 스키마 변경은 반드시 마이그레이션 파일로 관리
- Edge Functions는 민감한 로직에만 사용 (클라이언트 노출 방지)
- 환경변수: `NEXT_PUBLIC_` 접두사 규칙 엄수

### Vercel 배포 원칙
- `main` 브랜치 → 프로덕션 자동 배포
- `dev` 브랜치 → Preview URL (스테이징)
- 배포 전 반드시 로컬 빌드 확인 (`npm run build`)
- 환경변수는 Vercel 대시보드에서 관리, `.env.local`은 절대 커밋 금지

---

## 2. 기획 → 완성 자동화 흐름

> 기획 파일 하나로 개발 착수부터 배포까지 일관되게 진행합니다.

### 기획 파일 포맷 (`tasks/spec.md`)

새 프로젝트 또는 기능 개발 시작 전 반드시 아래 포맷으로 spec.md를 작성합니다.
Claude가 이 파일을 읽고 자동으로 todo.md 생성 → 개발 착수합니다.

```markdown
# [기능명 / 프로젝트명]

## 목적
- 이 기능이 왜 필요한가 (1~3줄)

## 사용자 스토리
- ~로서, ~를 원한다, ~때문에
- (구체적인 사용 시나리오)

## 기능 요구사항
- [ ] 기능 1
- [ ] 기능 2
- [ ] 기능 3

## 비기능 요구사항
- 성능: (응답속도, 동시접속 등)
- 보안: (인증, 권한, 데이터 보호)
- 제약: (사용 불가 라이브러리, 필수 호환성 등)

## 데이터 모델 (아는 만큼)
- 테이블명: 필드 목록

## UI/UX 방향 (아는 만큼)
- 페이지 구조
- 핵심 인터랙션

## 완료 기준 (Definition of Done)
- [ ] 로컬에서 정상 작동
- [ ] 빌드 오류 없음
- [ ] Vercel Preview 배포 확인
- [ ] 핵심 케이스 테스트 통과
```

### 자동화 흐름

```
1. spec.md 작성 (기획자/대표)
         ↓
2. Claude가 spec.md 읽기
         ↓
3. tasks/todo.md 자동 생성 (단계별 체크리스트)
         ↓
4. 구현 착수 (서브에이전트 병렬 처리)
         ↓
5. 단계별 검증 (빌드 → 로컬 테스트 → 오류 수정)
         ↓
6. Vercel Preview 배포 확인
         ↓
7. tasks/todo.md 완료 처리 + lessons.md 업데이트
         ↓
8. main 브랜치 머지 → 프로덕션 배포
```

---

## 3. Workflow Orchestration

### 3-1. Plan Node (기본 원칙)
- 3단계 이상이거나 아키텍처 결정이 필요한 모든 작업 → Plan Mode 진입
- spec.md → todo.md 변환을 가장 먼저 수행
- 문제가 생기면 즉시 STOP → 재플래닝 (억지로 밀어붙이지 않음)
- 검증 단계도 Plan에 포함 (구현만 계획하지 않음)

### 3-2. 서브에이전트 전략

**반드시 서브에이전트를 쓰는 트리거 조건:**
```
- DB 스키마 설계 + UI 컴포넌트 작업이 동시에 필요할 때
- 3개 이상의 독립적인 기능 모듈을 병렬 개발할 때
- 리서치(라이브러리 조사) + 구현이 동시에 필요할 때
- 버그 원인 탐색 + 다른 기능 개발을 병행할 때
- 테스트 작성 + 다음 기능 개발을 동시에 할 때
```

**서브에이전트 분리 원칙:**
- 1 서브에이전트 = 1 명확한 태스크
- 서브에이전트 간 의존성이 있으면 순서 명시
- 결과를 메인 컨텍스트에 통합하는 단계 반드시 포함

**서브에이전트 쓰지 않는 경우:**
- 단순 수정 (파일 1~2개, 10줄 이하 변경)
- 명확한 버그 수정 (원인이 이미 특정된 경우)

### 3-3. Self-Improvement Loop
- 사용자 수정/피드백 발생 즉시 → `tasks/lessons.md` 업데이트
- 같은 실수 2회 반복 금지 — lessons.md가 방어막
- 세션 시작 시 해당 프로젝트의 lessons.md 먼저 검토
- 패턴이 보이면 CLAUDE.md 자체를 업데이트 제안

### 3-4. 검증 루틴 (완료 전 필수)

```bash
# Step 1. 타입 체크
npx tsc --noEmit

# Step 2. 빌드 확인
npm run build

# Step 3. 로컬 실행 확인
npm run dev
# → 브라우저에서 직접 확인

# Step 4. Supabase 관련 변경사항
# - RLS 정책 적용 여부 확인
# - 마이그레이션 파일 생성 여부 확인

# Step 5. 환경변수 확인
# - .env.local 에 필요한 키 모두 있는지
# - Vercel 대시보드에도 동일하게 설정되어 있는지
```

**완료 기준 체크리스트:**
- [ ] `npx tsc --noEmit` — 오류 없음
- [ ] `npm run build` — 빌드 성공
- [ ] 핵심 기능 로컬에서 직접 확인
- [ ] Supabase RLS 적용 확인
- [ ] 환경변수 누락 없음
- [ ] Git 커밋 메시지 명확하게

### 3-5. 자율 버그 수정
- 버그 리포트 받으면 즉시 수정 — 방법 묻지 않음
- 로그, 에러 메시지, 스택트레이스 → 직접 분석 후 해결
- 수정 후 반드시 검증 루틴 실행
- 근본 원인(Root Cause) 파악 후 수정 — 증상만 덮지 않음

---

## 4. Task Management

### 파일 구조
```
tasks/
├── spec.md      ← 기획 (인풋) — 이 파일을 읽고 개발 시작
├── todo.md      ← 작업 목록 (자동 생성 + 진행 추적)
└── lessons.md   ← 실수 패턴 누적 (세션 시작 시 항상 검토)
```

### todo.md 포맷
```markdown
# [기능명] 작업 목록

## 진행 상황
- 시작: YYYY-MM-DD
- 상태: 진행중 / 완료 / 블로킹

## 작업 항목
### Phase 1. 기반 작업
- [ ] DB 스키마 설계
- [ ] Supabase 테이블 생성 + RLS 설정
- [ ] 환경변수 설정

### Phase 2. 핵심 기능 구현
- [ ] API 라우트 작성
- [ ] 컴포넌트 작성
- [ ] 상태 관리 연결

### Phase 3. 검증 및 배포
- [ ] 타입 체크
- [ ] 빌드 확인
- [ ] 로컬 테스트
- [ ] Vercel Preview 확인
- [ ] main 머지

## 완료 후 리뷰
- 잘 된 점:
- 개선할 점:
- lessons.md에 추가할 내용:
```

### lessons.md 포맷
```markdown
# 개발 패턴 & 교훈

## [날짜] [상황]
- **문제**: 어떤 실수/오류가 발생했는가
- **원인**: 왜 발생했는가
- **해결**: 어떻게 해결했는가
- **규칙**: 앞으로 이렇게 한다

---
```

---

## 5. 코드 품질 원칙

### 핵심 원칙
- **Simplicity First**: 가장 단순한 해결책 우선 — 과도한 추상화 금지
- **No Laziness**: 임시방편 없음, 근본 원인 해결
- **Minimal Impact**: 변경 범위 최소화 — 필요한 것만 건드림
- **Staff Engineer Standard**: "시니어 엔지니어가 PR 승인할 수 있는가?"

### Next.js 특화 원칙
- Server Component 우선 — 클라이언트 컴포넌트는 필요할 때만
- `use client` 는 인터랙션이 필요한 최소 단위에만
- API Route는 Supabase 직접 호출로 대체 가능하면 생략
- 이미지는 항상 `next/image` 사용

### Supabase 특화 원칙
- 클라이언트에서 직접 DB 접근 시 RLS로 보안 보장
- 민감한 로직은 Edge Function 또는 Server Action으로
- 실시간 구독은 필요한 컴포넌트에서만 (성능 고려)
- 타입은 `supabase gen types` 로 자동 생성하여 사용

### 우아함 원칙 (Elegance)
- 비자명한 변경에는 항상 "더 우아한 방법이 있는가?" 자문
- 해킹처럼 느껴지는 수정 → "지금 아는 모든 것을 알고 있다면, 어떻게 구현할까?"
- 단순하고 명확한 수정은 과도하게 고민하지 않음

---

## 6. 세션 시작 루틴

```
1. 이 파일(CLAUDE.md) 읽기
2. tasks/lessons.md 검토 (해당 프로젝트)
3. tasks/todo.md 현재 상태 확인
4. 프로젝트 도메인/산업 컨텍스트 파악 (세션에서 전달받음)
5. 작업 착수
```

---

## 7. Git 커밋 컨벤션

```
feat: 새로운 기능 추가
fix: 버그 수정
refactor: 리팩토링 (기능 변화 없음)
style: 스타일 변경 (로직 변화 없음)
chore: 설정, 패키지 등 기타 변경
docs: 문서 업데이트

예시:
feat: 사용자 인증 플로우 구현 (Supabase Auth)
fix: RLS 정책 누락으로 인한 데이터 노출 수정
```

---

*최종 업데이트: 2026년 3월*
*변경 시 수정 내용과 날짜를 하단에 기록*
